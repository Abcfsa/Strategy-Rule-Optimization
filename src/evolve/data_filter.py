"""p1 方差分解数据筛选（报告 §4.7.2，必做消融 E9）。

进化前预处理：
    Var[reward] = 候选间方差（真实质量差异） + 响应方差（生成随机性）
    采样 N 个候选策略 × 每个训练样本 M 次响应 → 估计每个样本的候选间方差
    → 保留方差最高的 K_top 个样本用于进化与蒸馏

依据：p1 论文证明全量训练样本会稀释信号（GEPA 记忆化倾向的直接证据），
在 AIME 上用 2/30 数据超全量 +7.14 分。
"""
