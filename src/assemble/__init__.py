"""sro.assemble — prompt 组装与三层检索。

对应技术路线报告 §4.3：
    p(x) = [ 全局策略 S ; retrieve_rules(R, x) ; retrieve_examples(E, x) ]

分层检索（控制开销）：
    1. 粗筛：问题文本与规则/支持集的语义相似度（embedding），取 Top-20
    2. 有效性加权：乘规则置信度分数（验证通过次数/使用次数）
    3. 错误模式信号：快速适应模式下用错误分类标签加权匹配
    4. 精选：对 Top-20 做一次 LLM 批处理打分，选最终注入的
       ≤5 条规则 + ≤3 条示例

预算控制（§6 风险对策，AgentBench 上下文天花板支撑）：
    硬长度预算：策略 ≤1500 字、规则 ≤5×150 字、示例 ≤3×500 字
    检索为空时只用策略层（宁缺毋滥）

难度分流（§4.7.4，Optimal Scaling）：简单题只注入策略层不检索规则。
"""
